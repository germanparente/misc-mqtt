#ifndef CONFIG_H
#define CONFIG_H

/* wifi settings */ 
const char* ssid     = <wifi sid>;
const char* password = <wifi password>;



/* broker settings  */
/* broker hostname */
const char* broker_host = <mybroker.example.com>;
/* broker port */
const int broker_port   = 1883;

/* broker user and password */
const char* broker_pwd =  <broker password>;
const char* broker_usr =  <broker user>;

#endif
